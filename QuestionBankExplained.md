# 질문이 왜 세션마다 18~20개만 나오나요?

"전체 질문은 216개인데 왜 화면엔 18개(혹은 20개)만 보이지?"에 대한 설명입니다.
`npm run merge-data`로 질문을 추가할 때마다 아래 숫자는 바뀔 수 있으니, 참고용 스냅샷입니다
(마지막 갱신 기준 총 216개, 2026-09-02).

**2026-09-02 업데이트**: 화면의 "질문 X / Y" 표시가 꼬리질문을 전혀 세지 않던 버그를
고쳤습니다 — 이제 꼬리질문이 나올 때마다 Y가 실제로 늘어나고, X도 그만큼 함께 올라갑니다
(예: 대분류 질문에 답했는데 꼬리질문이 이어지면 "질문 2 / 19"처럼 즉시 반영됨). 아래 §2의
poolSize는 "시작 시점의 최소 보장 개수"이고, 실제로 보는 총 개수는 꼬리질문이 얼마나
이어지느냐에 따라 세션 중에 계속 늘어납니다.

**2026-09-02 추가 업데이트**: "일본생활/걱정/좋아하는 점/회사 고르는 기준" 등 대분류 질문
6개를 새로 추가했고(§1 숫자 반영), 비슷한 주제의 질문끼리 묶어서 한 세션에 절대 함께
나오지 않게 하는 **그룹(group) 메커니즘**을 새로 도입했습니다 — 자세한 내용은 아래 §4.

---

## 1. 질문 은행(216개)은 세 종류로 나뉩니다

| 종류 | 태그 | 개수 | 언제 나오나 |
|---|---|---|---|
| **대분류 질문** | (태그 없음) | 149개 | 세션 시작 시 무작위로 뽑히는 "첫 질문" 풀 |
| **꼬리질문 전용** | `follow_up` | 62개 | 답변에 특정 키워드가 나왔을 때만 이어서 등장 |
| **마무리 전용** | `closing` | 5개 | "마지막 질문하기" 버튼을 눌렀을 때만 무작위로 1개 |

대분류 질문 149개의 카테고리 구성:
- personality(인성) 38개
- technical(기술) 57개
- culture_fit(컬처핏) 53개
- reverse(역질문) 1개

꼬리질문 전용 62개는 `public/data/follow_ups.txt`의 95개 규칙(원래질문 → 키워드 → 다음질문)을
통해서만 등장합니다. 즉 **216개 전부가 "세션당 나올 수 있는 질문"이 아니라, 149개는 시작 질문
후보, 62개는 조건부로만 등장하는 꼬리질문, 5개는 마무리 버튼 전용**입니다.

---

## 2. 화면에 "질문 X / Y"로 보이는 Y는 어떻게 정해지나요

세션이 시작될 때, 대분류 질문 풀(149개)에서 **모드마다 정해진 개수만큼** 무작위로 뽑습니다
(`features/interview/hooks/useInterviewMachine.ts`의 `poolSize`, 실제로는 아래 §4의
`sampleMainQuestions()`가 그룹 중복까지 걸러서 뽑습니다). 이건 "시작 시점의 최소 개수"이고,
세션 도중 꼬리질문이 나올 때마다 실제 Y는 이보다 계속 늘어납니다(위 안내 참고):

| 모드 | 뽑는 범위 | poolSize(시작 시 최소) |
|---|---|---|
| 연습 모드 | personality+technical+culture_fit (148개, reverse 제외) | 20 |
| 기술 면접 | technical만 (57개) | 18 |
| 실전 모드 | personality+technical+culture_fit (148개, reverse 제외) | 자기소개(1) + 16 + 역질문(1) = 18 |

poolSize를 각 카테고리 전체 개수보다 낮게 두는 이유: 풀 크기에 너무 가까우면 세션마다 거의
같은 질문 조합만 나오게 되기 때문입니다. 카테고리별 개수는 질문을 추가할 때마다 바뀌므로,
`npm run merge-data` 실행 후 이 문서 §1의 표도 함께 갱신하는 것을 권장합니다.

**여기에 더해, 답변 중 키워드가 걸리면 꼬리질문이 큐에 실시간으로 추가됩니다** — 그래서 실제
진행하다 보면 표시되는 총 개수(Y)가 처음 화면보다 늘어나는 것을 볼 수 있습니다. "꼬리질문
종료" 버튼을 누르면 그 자리에서 꼬리질문 체인을 끊고 바로 다음 대분류 질문으로 넘어갑니다.

---

## 3. 꼬리질문은 실제로 몇 단계까지 이어지나요

`public/data/follow_ups.txt`는 "원래질문 → 키워드 → 다음질문" 규칙만 있는데, **다음질문
자체도 또 다른 규칙의 "원래질문"이 될 수 있어서 자연스럽게 여러 단계로 이어집니다.**

예시 (2025-09-01 batch4로 추가한 "팀 프로젝트 갈등" 체인):
```
team_project(팀 프로젝트 경험은?)
  └─ 답변에 "対立/意見が分かれ" 포함 → tp_conflict_tier1(의견 대립 시 어떻게 해결?)
       └─ 답변에 "納得/データ" 포함 → tp_conflict_tier2(상대가 납득 안 하면?)
            └─ 답변에 "上司/先輩" 포함 → tp_conflict_tier3(상사·선배 의견이 다르면?)
```
이렇게 3단계까지 이어지는 체인이 여러 개 있습니다(2025-09-01 batch4에서 5개 대분류 ×
1~3단계 총 28개 꼬리질문 추가). 다만 **키워드가 실제로 답변에 등장해야만** 다음 단계로
넘어가므로, 매번 끝까지 이어지지는 않습니다 — AI가 아니라 규칙(키워드 매칭) 기반이라
그렇습니다(0원 운영 원칙, `readme_3.md` §7 참고).

같은 대상 질문은 세션당 한 번만 나오도록 막혀 있어(`askedFollowUpsRef`), 무한 루프 걱정은
없습니다.

---

## 4. 비슷한 질문이 한 세션에 같이 나오지 않게 하는 group 필드

"스트레스 해소법이 뭔가요"와 "스트레스에 어떻게 대처하나요"처럼 사실상 같은 질문이 한
세션에서 둘 다 나오면 어색합니다. 이를 막기 위해 `data/questions.json`의 질문에 선택적
`group` 필드를 추가했습니다 — 같은 `group` 값을 가진 질문들은 **한 세션에 그 중 하나만**
무작위로 뽑힙니다 (`lib/questionBank.ts`의 `sampleMainQuestions()`). `group`이 없는
질문은 자기 자신의 `id`가 곧 그룹이라, 다른 질문과 절대 묶이지 않습니다.

2026-09-02 기준으로 묶인 그룹(각 2개씩):

| group | 묶인 질문 |
|---|---|
| `stress_relief` | stress, stress_relief_method |
| `school_effort` | effort_in_school_days, childhood_effort |
| `hobby` | hobby, day_off_activity |
| `japan_life_adapt` | adapt_japan_life, life_in_japan_experience |
| `japan_impression` | japan_impression, japan_like_point |
| `years_in_japan` | years_in_japan, years_in_japan_career |

새 질문을 추가할 때 기존 질문과 주제가 거의 겹친다면, `data/questions.json`에서 두 질문에
같은 `group` 문자열만 넣어주면 됩니다(스키마 변경 없이 바로 적용됩니다). merge-data 스크립트는
이 필드를 그대로 통과시키므로, `data/drafts/*.json` 초안 파일에서부터 `group`을 넣어도 됩니다.

---

## 5. 질문을 더 추가하고 싶다면

`HowToInputData.md`를 참고하세요 — `data/` 폴더에 초안 파일을 만들고
`npm run merge-data`만 실행하면 자동으로 병합됩니다. 새 대분류 질문뿐 아니라, 여기서 설명한
것과 같은 "키워드 → 꼬리질문" 다단계 체인도 같은 방식으로 추가할 수 있습니다
(`HowToInputData.md` §7에 실전 예시가 있습니다).

세션당 보이는 개수(poolSize) 자체를 더 늘리거나 줄이고 싶다면
`features/interview/hooks/useInterviewMachine.ts`의 `poolSize` 값만 바꾸면 됩니다.
