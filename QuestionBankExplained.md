# 질문이 왜 세션마다 18~20개만 나오나요?

"전체 질문은 247개인데 왜 화면엔 18개(혹은 20개)만 보이지?"에 대한 설명입니다.
`npm run merge-data`로 질문을 추가할 때마다 아래 숫자는 바뀔 수 있으니, 참고용 스냅샷입니다
(마지막 갱신 기준 총 247개, 대분류 215개, 2026-09-02).

**2026-09-02 추가 업데이트 (5)**: "꼬리질문이 잘 안 나온다, 다양하게 안 느껴진다"는 요청으로
꼬리질문 전용 67개 중 **문맥 의존 대명사(その/それ/この) 없이 그 자체로 자연스러운 40개를
대분류로 승격**했습니다(대분류 175→215개, 꼬리질문 전용 67→27개). 승격된 질문도
follow_ups.txt의 대상으로는 그대로 남아있어서, 키워드가 실제로 걸리면 지금처럼 꼬리질문으로도
등장하고 그때는 대분류 풀의 자리가 자동으로 빠집니다. 자세한 내용은 아래 §7.

**2026-09-02 추가 업데이트 (4)**: 실전 모드 지원 직무에 "기본"을 추가했습니다(소프트웨어/
반도체/기본 3택). 기본 모드는 다른 두 트랙과 달리 큰 풀에서 무작위로 뽑지 않고, 실제
면접에서 거의 항상 나오는 대표 질문 12개(自己紹介 제외, 最後に一言 제외)를 정해진 순서
그대로 씁니다 — 자세한 내용은 아래 §6.

**2026-09-02 업데이트**: 화면의 "질문 X / Y" 표시가 꼬리질문을 전혀 세지 않던 버그를
고쳤습니다 — 이제 꼬리질문이 나올 때마다 Y가 실제로 늘어나고, X도 그만큼 함께 올라갑니다
(예: 대분류 질문에 답했는데 꼬리질문이 이어지면 "질문 2 / 19"처럼 즉시 반영됨). 아래 §2의
poolSize는 "시작 시점의 최소 보장 개수"이고, 실제로 보는 총 개수는 꼬리질문이 얼마나
이어지느냐에 따라 세션 중에 계속 늘어납니다.

**2026-09-02 추가 업데이트 (1)**: "일본생활/걱정/좋아하는 점/회사 고르는 기준" 등 대분류 질문
6개를 새로 추가했고, 비슷한 주제의 질문끼리 묶어서 한 세션에 절대 함께 나오지 않게 하는
**그룹(group) 메커니즘**을 새로 도입했습니다 — 자세한 내용은 아래 §4.

**2026-09-02 추가 업데이트 (2)**: 대분류 질문을 21개 더 추가하고(소프트웨어/반도체 지원
직무별 전용 질문 포함), **지원 직무(track) 필터링 메커니즘**을 도입했습니다 — 실전 모드
시작 시 "소프트웨어"/"반도체" 중 지원 직무를 고르면, 다른 직무 전용 질문은 그 세션에서
아예 나오지 않습니다. 자세한 내용은 아래 §5.

**2026-09-02 추가 업데이트 (3)**: 헤더의 "(전체 Z개)" 표시가 꼬리질문·마무리 전용까지 섞인
숫자(242)라 "세션당 몇 개 나오나"와 혼동되기 쉬웠던 것을 "대분류 총 Z개"(170, 대분류만)로
바꿨습니다. 그리고 poolSize를 대폭 늘리고(연습 20→30, 기술면접 18→24, 실전 16→28),
**중요한 꼬리질문인데 키워드가 안 나오면 못 물어보는 문제**를 완화하는 두 가지 장치를
추가했습니다 — 자세한 내용은 아래 §3.

---

## 1. 질문 은행(247개)은 세 종류로 나뉩니다

| 종류 | 태그 | 개수 | 언제 나오나 |
|---|---|---|---|
| **대분류 질문** | (태그 없음) | 215개 | 세션 시작 시 무작위로 뽑히는 "첫 질문" 풀 |
| **꼬리질문 전용** | `follow_up` | 27개 | 답변에 특정 키워드가 나왔을 때만 이어서 등장 |
| **마무리 전용** | `closing` | 5개 | "마지막 질문하기" 버튼을 눌렀을 때만 무작위로 1개 |

대분류 질문 215개의 카테고리 구성:
- personality(인성) 57개
- technical(기술) 79개 — 공통 20개 + 소프트웨어 트랙 전용 51개 + 반도체 트랙 전용 4개
- culture_fit(컬처핏) 78개
- reverse(역질문) 1개

이 중 12개는 `basic_track` 태그가 붙어 있어 "기본 모드" 전용 고정 목록으로도 쓰입니다
(§6 참고) — 대분류 풀에서 무작위로도 뽑힐 수 있고, 기본 모드에서는 항상 이 12개만 정해진
순서로 나옵니다.

꼬리질문 전용 27개는 `public/data/follow_ups.txt`의 89개 규칙(원래질문 → 키워드 → 다음질문)을
통해서만 등장합니다. 즉 **247개 전부가 "세션당 나올 수 있는 질문"이 아니라, 215개는 시작 질문
후보, 27개는 조건부로만 등장하는 꼬리질문, 5개는 마무리 버튼 전용**입니다(다만 27개 중
상당수는 대분류로 승격된 40개와 마찬가지로 여전히 follow_ups.txt의 대상이기도 해서,
키워드가 걸리면 지금처럼 등장합니다 — §8 참고).

화면 헤더에는 "(전체 247개)"가 아니라 **"(대분류 총 215개)"**가 표시됩니다 — 꼬리질문·마무리
전용까지 섞인 숫자는 "세션당 몇 개 나오나"를 묻는 질문에 오히려 오해를 주기 쉬워서
(2026-09-02), 대분류 개수만 보여주도록 `lib/questionBank.ts`에 `getMainQuestionCount()`를
따로 만들어 바꿨습니다.

---

## 2. 화면에 "질문 X / Y"로 보이는 Y는 어떻게 정해지나요

세션이 시작될 때, 대분류 질문 풀(215개)에서 **모드마다 정해진 개수만큼** 무작위로 뽑습니다
(`features/interview/hooks/useInterviewMachine.ts`의 `poolSize`, 실제로는 아래 §4의
`sampleMainQuestions()`가 그룹 중복·지원 직무(track) 불일치까지 걸러서 뽑습니다). 이건
"시작 시점의 최소 개수"이고, 세션 도중 꼬리질문이 나올 때마다 실제 Y는 이보다 계속
늘어납니다(위 안내 참고):

| 모드 | 뽑는 범위 | poolSize(시작 시 최소) |
|---|---|---|
| 연습 모드 | personality+technical+culture_fit (214개, reverse 제외, 트랙 필터 없음) | 30 |
| 기술 면접 | technical만 (79개, 트랙 필터 없음) | 24 |
| 실전 모드(소프트웨어) | 위 214개 중 소프트웨어 트랙에 맞는 것(약 205개) | 자기소개(1) + 28 + 역질문(1) = 30 |
| 실전 모드(반도체) | 위 214개 중 반도체 트랙에 맞는 것(약 159개) | 자기소개(1) + 28 + 역질문(1) = 30 |
| 실전 모드(기본) | 무작위 아님 — `basic_track` 태그 12개 고정(§6) | 자기소개(1) + 12 + 마지막 한마디(1) = 14 |

(2026-09-02: "16개는 너무 적다, 꼬리질문 제외 30개 정도는 되어야 한다"는 요청으로 세 모드
모두 대폭 늘렸습니다. 각 모드의 후보 풀이 그룹 중복을 걸러도 여전히 poolSize의 5배 이상
남아있어서, 이 정도로 늘려도 세션마다 조합이 충분히 다양합니다.)

연습 모드와 기술 면접 모드는 지원 직무를 묻지 않아서 트랙 필터링 없이 모든 질문이 후보가
됩니다(§5). 실전 모드만 시작 화면에서 소프트웨어/반도체를 먼저 고르고, 그 결과로 후보 풀이
줄어듭니다.

poolSize를 각 카테고리 전체 개수보다 낮게 두는 이유: 풀 크기에 너무 가까우면 세션마다 거의
같은 질문 조합만 나오게 되기 때문입니다. 카테고리별 개수는 질문을 추가할 때마다 바뀌므로,
`npm run merge-data` 실행 후 이 문서 §1의 표도 함께 갱신하는 것을 권장합니다.

**여기에 더해, 답변 중 키워드가 걸리면 꼬리질문이 큐에 실시간으로 추가됩니다** — 그래서 실제
진행하다 보면 표시되는 총 개수(Y)가 처음 화면보다 늘어나는 것을 볼 수 있습니다. "꼬리질문
종료" 버튼을 누르면 그 자리에서 꼬리질문 체인을 끊고 바로 다음 대분류 질문으로 넘어갑니다.

---

## 3. 꼬리질문은 실제로 몇 단계까지 이어지나요

`public/data/follow_ups.txt`는 "원래질문 → 키워드 → 다음질문" 규칙만 있는데, **다음질문
자체도 또 다른 규칙의 "원래질문"이 될 수 있어서 자연스럽게 여러 단계로 이어집니다.**

예시 (2025-09-01 batch4로 추가한 "팀 프로젝트 갈등" 체인):
```
team_project(팀 프로젝트 경험은?)
  └─ 답변에 "対立/意見が分かれ" 포함 → tp_conflict_tier1(의견 대립 시 어떻게 해결?)
       └─ 답변에 "納得/データ" 포함 → tp_conflict_tier2(상대가 납득 안 하면?)
            └─ 답변에 "上司/先輩" 포함 → tp_conflict_tier3(상사·선배 의견이 다르면?)
```
이렇게 3단계까지 이어지는 체인이 여러 개 있습니다(2025-09-01 batch4에서 5개 대분류 ×
1~3단계 총 28개 꼬리질문 추가). 다만 **키워드가 실제로 답변에 등장해야만** 다음 단계로
넘어가므로, 매번 끝까지 이어지지는 않습니다 — AI가 아니라 규칙(키워드 매칭) 기반이라
그렇습니다(0원 운영 원칙, `readme_3.md` §7 참고).

같은 대상 질문은 세션당 한 번만 나오도록 막혀 있어(`askedFollowUpsRef`), 무한 루프 걱정은
없습니다.

**2026-09-02 추가: "핵심 꼬리질문인데 키워드를 안 말하면 영영 못 물어보는" 문제 완화**

키워드 매칭 방식의 근본적인 한계로, 정말 중요한 꼬리질문이라도 사용자가 그 정확한 단어를
말하지 않으면 그 세션에서는 절대 등장하지 않습니다. 이를 두 가지 방법으로 완화했습니다.

1. **안전망(`*`) 규칙** — `public/data/follow_ups.txt`에서 감지할단어 칸에 `*` 하나만 쓰면,
   다른 키워드 규칙이 전부 안 맞았을 때만 마지막으로 검사해서 **50% 확률로라도** 그 꼬리질문을
   물어봅니다(`lib/followUp.ts`의 `triggerType: 'random'`을 텍스트 파일에서 쓸 수 있게
   `parseFollowUpsText`를 고쳤습니다). 우선순위를 `-1`처럼 낮게 줘서 진짜 키워드 매칭이
   항상 먼저 시도되고, 아무것도 안 걸렸을 때만 이 안전망이 개입합니다. 2026-09-02 기준
   적용된 안전망: `team_project→tp_conflict_tier1`, `why_this_company→why_not_other_company`,
   `motivation→motiv_strength_tier1`, `problem_solving→how_solved`,
   `opinion_conflict_communication`/`conflict`→`opinion_split_decision`,
   `setback_experience`/`failure_experience_general`→`fail_cause_tier1`.
2. **대분류 질문을 꼬리질문으로 선점하기(예: 자기소개→팀 프로젝트)** — 자기소개 답변에서
   "チーム" 같은 단어가 나오면, 세션 풀에 이미 예정되어 있을 수도 있는 `team_project`(대분류
   질문)를 그 자리에서 바로 꼬리질문으로 물어봅니다(`self_intro | チーム,... | team_project`).
   대분류 질문도 얼마든지 꼬리질문의 "다음질문"으로 지정할 수 있습니다 — 태그와 무관하게
   id로만 연결되기 때문입니다. 다만 이렇게 하면 나중에 세션 풀에서 또 같은 질문이 나올
   위험이 있어서, `useInterviewMachine.ts`의 `confirmAnswer`가 꼬리질문을 큐에 끼워 넣기 전에
   **같은 id가 이후 순서에 남아있으면 미리 제거**하도록 고쳐서 같은 질문이 두 번 나오지
   않게 만들었습니다. 이 패턴은 다른 주제로도 똑같이 확장할 수 있습니다(예: `self_intro`에서
   "아르바이트"가 나오면 `part_time_job`을 바로 꼬리질문으로).

---

## 4. 비슷한 질문이 한 세션에 같이 나오지 않게 하는 group 필드

"스트레스 해소법이 뭔가요"와 "스트레스에 어떻게 대처하나요"처럼 사실상 같은 질문이 한
세션에서 둘 다 나오면 어색합니다. 이를 막기 위해 `data/questions.json`의 질문에 선택적
`group` 필드를 추가했습니다 — 같은 `group` 값을 가진 질문들은 **한 세션에 그 중 하나만**
무작위로 뽑힙니다 (`lib/questionBank.ts`의 `sampleMainQuestions()`). `group`이 없는
질문은 자기 자신의 `id`가 곧 그룹이라, 다른 질문과 절대 묶이지 않습니다.

2026-09-02 기준으로 묶인 그룹(각 2개씩):

| group | 묶인 질문 |
|---|---|
| `stress_relief` | stress, stress_relief_method |
| `school_effort` | effort_in_school_days, childhood_effort |
| `hobby` | hobby, day_off_activity |
| `japan_life_adapt` | adapt_japan_life, life_in_japan_experience |
| `japan_impression` | japan_impression, japan_like_point |
| `years_in_japan` | years_in_japan, years_in_japan_career |

새 질문을 추가할 때 기존 질문과 주제가 거의 겹친다면, `data/questions.json`에서 두 질문에
같은 `group` 문자열만 넣어주면 됩니다(스키마 변경 없이 바로 적용됩니다). merge-data 스크립트는
이 필드를 그대로 통과시키므로, `data/drafts/*.json` 초안 파일에서부터 `group`을 넣어도 됩니다.

---

## 5. 지원 직무(소프트웨어/반도체)에 안 맞는 질문은 어떻게 걸러지나요

실전 모드를 시작할 때 "지원 직무를 골라주세요" 화면에서 소프트웨어/반도체 중 하나를 고르면
(`app/interview/page.tsx`), 그 선택이 URL 쿼리(`?mode=real&track=software`)로 면접 화면에
전달되고, `useInterviewMachine`이 `sampleMainQuestions(categories, poolSize, track)`을
호출할 때 그대로 넘어갑니다.

질문마다 선택적 `track` 필드(`'software' | 'semiconductor'`)를 가질 수 있습니다:
- `track`이 없는 질문(자기소개, 지원동기, 스트레스 해소법, 팀 프로젝트 경험 등 대부분의
  인성/컬처핏 질문과 일부 범용 기술 질문) → **어떤 트랙에서도** 그대로 나옵니다.
- `track: 'software'`인 질문(Git/CI-CD/Docker/코드리뷰/머신러닝·LLM 프로젝트 등 46개) →
  반도체 지원자에게는 나오지 않습니다.
- `track: 'semiconductor'`인 질문(반도체 제조공정, 클린룸, 장비 트러블 대응, 客先常駐 등
  9개) → 소프트웨어 지원자에게는 나오지 않습니다.

연습 모드와 기술 면접 모드는 지원 직무를 묻지 않으므로, 이 필터링 없이 모든 track의 질문이
섞여서 나올 수 있습니다 — "여러 직무를 두루 연습해보고 싶을 때"는 이 두 모드를 쓰면 됩니다.
실전 모드처럼 실제 지원하는 직무 하나에 맞춰 연습하고 싶을 때만 트랙을 고르면 됩니다.

새 질문을 추가할 때 특정 직무에만 해당한다면 `data/questions.json`에 `"track": "software"`
또는 `"track": "semiconductor"`만 추가하면 됩니다(다른 직무를 새로 늘리고 싶다면 `JobTrack`
타입(`lib/questionBank.ts`)에 값을 추가하고, `app/interview/page.tsx`의 `JOB_TRACKS`
배열에도 버튼을 추가하면 됩니다).

---

## 6. "기본 모드"는 왜 소프트웨어/반도체와 다르게 동작하나요

소프트웨어/반도체 트랙은 큰 풀(214개)에서 매번 무작위로 뽑지만, **기본 모드는 무작위가
아닙니다.** 실제 면접에서 거의 항상 나오는 대표 질문 12개를 `lib/questionBank.ts`의
`BASIC_TRACK_QUESTION_IDS`에 정해진 순서 그대로 고정해뒀고, `getBasicTrackQuestions()`가
이 순서 그대로 반환합니다:

1. 当社でどのような仕事をしたいですか。(job_role_desired)
2. 志望動機を教えてください。(motivation)
3. 当社の求人をどのように知りましたか。(how_found_job_posting)
4. なぜ日本で働きたいのですか。(why_japan)
5. 就職先の会社を選ぶとき、何を基準にしていますか。(company_choice_criteria)
6. なぜこの業界に興味を持ったのですか。(why_this_industry)
7. 学生時代に最も力を入れたことは何ですか。(effort_in_school_days)
8. サークル活動・アルバイト経験など、学業以外の活動について教えてください。(extracurricular_activities)
9. 今まで一番苦労したことは何ですか。(hardship_experience)
10. 長所と短所を教えてください。(strengths_and_weaknesses)
11. 日本ＩＴ研修に参加した理由を教えてください。(it_training_reason)
12. 入社後の抱負を教えてください。(post_join_aspiration)

이 12개는 전부 `data/questions.json`에 `"basic_track"` 태그가 붙어 있습니다(개중 7개는
원래 있던 대분류 질문을 재사용한 것이고, 5개는 이번에 새로 추가한 것입니다). 앞뒤로는
다른 트랙과 마찬가지로 자기소개(`REAL_MODE_INTRO_QUESTION`, 항상 첫 질문)가 붙지만,
**마무리는 역질문이 아니라 "最後に一言お願いします。"(`final_word`)로 끝납니다** — 소프트웨어/
반도체 트랙만 "最後に、何か質問はありますか。"(역질문)로 마무리합니다
(`features/interview/hooks/useInterviewMachine.ts`의 `isBasicTrack` 분기 참고).

목록을 바꾸고 싶으면 `BASIC_TRACK_QUESTION_IDS` 배열의 순서를 바꾸거나 id를 추가/삭제하면
됩니다 — 이 배열에 있는 순서 그대로 나오므로, 무작위 셔플이 필요 없다면 이 방식이 가장
간단합니다.

---

## 7. 꼬리질문이 대분류로 "승격"되는 경우도 있습니다

"꼬리질문이 잘 안 나오고, 대분류 질문만 도는 느낌이라 다양하게 안 느껴진다"는 피드백으로
2026-09-02에 꼬리질문 전용 67개 중 40개를 대분류로 옮겼습니다. 기준은 딱 하나입니다 —
질문 문장이 **その/それ/この/あの 같은 지시어로 직전 답변 내용을 가리키지 않고, 그 자체만
읽어도 자연스러운지**입니다. 예를 들어:
- `role_detail`("その中で、あなたが担当した部分は何ですか。")처럼 "その"가 가리키는 게
  직전 답변 없이는 알 수 없는 질문은 **꼬리질문 전용으로 남겨뒀습니다.**
- `tp_conflict_tier1`("チームメンバーの意見が強く対立したとき、具体的にどのように解決
  しましたか。")처럼 지시어 없이 그 자체로 완결된 질문(대개 "〜한 경험이 있습니까"/
  "〜라면 어떻게 하시겠습니까" 같은 가정형·경험형 문장)은 **대분류로 승격**했습니다.

승격된 질문도 `data/questions.json`에서 `follow_up` 태그만 빠졌을 뿐, id 자체는 그대로라
`follow_ups.txt`의 대상(다음질문)으로도 계속 유효합니다 — 즉 **두 가지 방식으로 모두
등장할 수 있습니다**: (a) 대분류 풀에서 무작위로 뽑혀서, 또는 (b) 부모 질문 답변에
키워드가 실제로 걸려서 꼬리질문으로. 같은 세션에서 (b)로 등장하면
`useInterviewMachine.ts`의 `confirmAnswer`가 (a)로 이미 예정되어 있던 자리를 자동으로
제거해서 같은 질문이 두 번 나오지 않습니다(이 중복 제거 로직은 원래 "자기소개→
team_project" 승격을 위해 만든 것을 그대로 재사용합니다).

승격되면서 기존 대분류 질문과 주제가 겹치게 된 것들은 group으로 묶었습니다(§4 참고,
예: `conflict`/`opinion_conflict_communication`/`opinion_split_decision` 세 개를
`team_opinion_conflict` 그룹으로, `weakness`/`strengths_and_weaknesses`를
`strengths_weaknesses` 그룹으로).

**남아있는 꼬리질문 전용 27개**는 대부분 tier2/tier3처럼 앞 답변의 구체적 내용을 가리키는
심화 질문이거나, 특정 기술/상황이 언급되어야만 말이 되는 질문입니다 — 이런 것들은 앞으로도
계속 꼬리질문으로만 등장하는 게 맞습니다(무리하게 승격하면 맥락 없이 어색한 질문이 나옵니다).

---

## 8. 질문을 더 추가하고 싶다면

`HowToInputData.md`를 참고하세요 — `data/` 폴더에 초안 파일을 만들고
`npm run merge-data`만 실행하면 자동으로 병합됩니다. 새 대분류 질문뿐 아니라, 여기서 설명한
것과 같은 "키워드 → 꼬리질문" 다단계 체인도 같은 방식으로 추가할 수 있습니다
(`HowToInputData.md` §7에 실전 예시가 있습니다).

세션당 보이는 개수(poolSize) 자체를 더 늘리거나 줄이고 싶다면
`features/interview/hooks/useInterviewMachine.ts`의 `poolSize` 값만 바꾸면 됩니다.
