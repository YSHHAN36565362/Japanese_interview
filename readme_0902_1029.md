# Voice Interview JP — 현재 구현 기능 총정리 (readme_4.md)

> `readme_3.md`가 "기획안"이었다면, 이 문서는 **2026-09-01 기준 실제로 코드에 구현되어 있는 기능**을
> 화면/기능/데이터 단위로 정리한 스냅샷입니다. 다른 탭에서 질문·꼬리질문 데이터를 작성하실 때
> 참고할 파일 위치도 이 문서 안에 모아뒀습니다 (§7). 문서 끝에는 최근 공유해주신 "Zoom형 프런트엔드
> 가이드"(Manus 작성) 대비 지금 구현이 어디까지 와 있는지도 정리했습니다 (§9).

---

## 1. 한눈에 보는 스택

| 영역 | 실제 사용 중인 것 |
|---|---|
| 프런트엔드 | Next.js 16.3.4 (App Router, Turbopack) + React 19.2.8 + TypeScript |
| 스타일 | 순수 CSS (`app/globals.css` 한 파일, Tailwind 미사용) |
| 백엔드/인증 | Supabase (`@supabase/supabase-js` 2.112.4, `@supabase/ssr` 0.12.5) — 익명 로그인 |
| 음성 | 브라우저 Web Speech API (`SpeechRecognition` STT, `SpeechSynthesis` TTS) |
| 화상/녹화 | 브라우저 `getUserMedia` + `MediaRecorder` (전부 클라이언트, 서버 업로드 없음) |
| 질문 데이터 | 로컬 파일 (`data/questions.json`, `public/data/follow_ups.txt`) — git으로 관리, Supabase 미사용 |
| 배포 대상 | Vercel (무료 Hobby) + Supabase (무료 티어) |

0원 운영 원칙은 계속 유지됩니다: LLM/AI API 호출이 코드 어디에도 없고, 모든 "지능적으로 보이는" 동작
(꼬리질문, 피드백, 레벨 추천)은 정규식/문자열 비교/규칙 기반입니다.

---

## 2. 인증 — 공유 비밀번호 + 익명 로그인

- 이메일을 전혀 사용하지 않습니다 (회원가입 확인 메일, 매직 링크 전부 없음).
- `app/login/page.tsx`에서 클라이언트 코드가 입력값을 고정 문자열(`SHARED_PASSWORD = 'kmove13'`)과
  비교합니다. 일치하면 `supabase.auth.signInAnonymously()`를 호출해 Supabase 익명 세션을 발급받습니다.
- Supabase 프로젝트에서 **Authentication → Anonymous Sign-Ins**가 켜져 있어야 동작합니다.
- 로그아웃: 마이페이지의 `LogoutButton` → `supabase.auth.signOut()`.
- 이 비밀번호는 클라이언트 코드에 그대로 있는 "가벼운 문지기"이며, 실제 계정 인증이 아닙니다
  (5~10명 규모 비공개 데모 용도).

---

## 3. 화면별 기능

### 3-1. 홈 (`/`)
- 기울어진 호버 카드(`HeroCard`)에 "JP · 日本語面接練習プログラム" 타이틀.
- 브라우저 STT/TTS 지원 여부를 실시간으로 검사해 배지로 표시 (`SupportBanner`).
- 로그인 여부에 따라 "비밀번호 입력하고 시작하기" 또는 "레벨 체크 시작 / 바로 면접 시작" 버튼.
- 배경에 카타카나 매트릭스 애니메이션(`MatrixBackground`), 콘텐츠는 macOS Safari 스타일 창
  (`MacWindow`)에 담겨 있음.

### 3-2. 레벨 체크 (`/level-check`)
로그인 후 진입. 4단계 진행:
1. **자가 신고**: JLPT 수준(N1~N5/모름), 경어 자신감, 장음 자신감을 세그먼트 버튼(`PillRadio`)으로 선택.
2. **장음 체크**: 장음 최소 대립쌍 단어(예: おばあさん/おばさん)를 듣고 따라 말하면, 인식된 텍스트가
   "짧게 발음했을 때의 오인식 형태"와 일치하는지 비교해 장음 위험 플래그를 계산.
3. **경어 문장 체크**: 정해진 경어 문장을 따라 말하면, 문자 단위 유사도(근사치)와 정중체 비율을 계산.
4. **결과**: 위 신호를 종합해 추천 난이도(N1~N5)와 경어 모드(강제/자율/보통체 허용)를 제시하고,
   `diagnostic_results`에 기록 + `user_settings`에 반영 후 `/interview`로 이동.
- 모든 계산은 문자열 비교 기반 근사치이며, 화면에 "추정치"로 명시되어 있습니다.
- ⚠️ 참고: 질문 은행 자체는 더 이상 JLPT로 필터링하지 않습니다(§4 참고). 이 레벨 체크의 JLPT 값은
  주로 `keigo_mode`(경어 강제/자율/허용) 추천에만 쓰입니다 — 실제 면접관은 지원자 수준과 무관하게
  항상 경어로 질문하기 때문에, 질문 난이도 자체를 나눌 필요가 없다고 판단했습니다.

### 3-3. 면접 모드 선택 (`/interview`)
- 4가지 모드: 연습 / 실전 / 기술 면접 / 역질문.
- 각 모드 카드는 macOS Safari 미니 창처럼 트래픽 라이트 타이틀바가 달린 버튼(`.mode-card`)입니다.
- 모드를 클릭하면 Supabase `sessions` 테이블에 새 세션 행을 만들고 `/interview/run/[sessionId]`로 이동.

### 3-4. 면접 진행 (`/interview/run/[sessionId]`) — Zoom형 면접실 (전면 리팩터링 완료)

`zoom_style_frontend_implementation_guide.md`(Manus 작성)를 기준으로 이 화면 전체를 다시 만들었습니다.
`app/interview/run/[sessionId]/page.tsx`는 이제 `sessionId`/`mode`만 읽어 `InterviewRoom`에 넘기는
얇은 로더이고, 실제 로직은 전부 `features/interview/` 아래로 분리되어 있습니다.

**폴더 구조**
```
features/interview/
  types.ts                    InterviewPhase / InterviewEvent 타입
  constants.ts                모드→카테고리 매핑, 상태별 안내 문구
  hooks/
    useInterviewMachine.ts    면접 진행의 중심 컨트롤러 (질문 큐, 답변 확정, 저장, 꼬리질문 판단)
    useMediaDevices.ts        마이크/카메라 스트림 관리 (카메라는 버튼 누를 때만 요청)
    useAudioLevel.ts          AnalyserNode 기반 음량 레벨 (평가용 아님, 시각 피드백 전용)
    useSpeechRecognition.ts   phase==="listening"일 때만 켜지는 STT (의도치 않은 재시작 방지)
    useSpeechSynthesis.ts     질문 낭독 TTS + onStart/onEnd → 상태 전이 연동
    useSessionTimer.ts        질문별 경과 시간 타이머
  lib/
    evaluateAnswer.ts         lib/feedback.ts 재노출
    followUpEngine.ts         lib/followUp.ts + lib/questionBank.ts 조합
    choonMatcher.ts           장음 오인식 비교 유틸(레벨체크와 공유)
    transcriptNormalizer.ts   승인된 STT 보정 사전을 답변에 자동 적용
  components/
    PreflightDialog.tsx       입장 전 장치/개인정보 안내 + 마이크 테스트
    RoomHeader.tsx            48px 상단바 (나가기·모드·레벨·경어모드·타이머·연결상태)
    InterviewStage.tsx        면접관 아바타·질문·자막·다시듣기
    SelfPreview.tsx           셀프 카메라 또는 파형/이니셜 카드 (스테이지 안에 도킹)
    TranscriptPanel.tsx       현재 답변 초안 편집 영역
    CoachingPanel.tsx         STAR/PREP 체크리스트 + 규칙 기반 신호(실시간 계산)
    RoomControls.tsx          76px 하단 고정 컨트롤 바
    InterviewRoom.tsx         위 조각들을 조립하는 화면 본체
```

**면접 상태 머신**
가이드가 제안한 `InterviewPhase`(`preflight → questionReady → interviewerSpeaking → listening →
answerReview → saving → completed`, 그리고 `fallbackText`)를 채택했습니다. TTS 재생 중에는 마이크를
켤 수 없고, "답변 완료"를 누르면 곧바로 다음 질문으로 넘어가지 않고 `answerReview`(전사 편집) 단계를
반드시 거친 뒤에만 저장·다음 질문 진행이 가능합니다. `followUpReady`는 별도 대기 화면 없이 저장 직후
즉시 다음 질문(꼬리질문 또는 다음 문항)으로 전환되도록 단순화했습니다(§9 참고).

**레이아웃 (데스크톱 3영역 + 고정 컨트롤)**
- 상단 48px `RoomHeader`, 하단 76px `RoomControls` 고정, 그 사이를 스테이지(가변폭)와 352px 보조
  패널이 채웁니다. 1280px 이상에서 패널이 그리드 컬럼으로 상시 표시되고, 768~1279px에서는 80% 폭
  드로어로, 767px 이하에서는 하단 시트로 전환됩니다(가이드 §2 표 그대로 CSS 미디어쿼리로 구현).
- 셀프 카메라/파형 미리보기는 스테이지 안에 도킹되며 크기도 같은 표를 따릅니다
  (240×135 / 200×112 / 112×150).
- 사이트 공통 상단바(macOS Safari 테마)는 이 경로(`/interview/run/*`)에서만 자동으로 숨겨집니다
  (`components/SiteChrome.tsx`가 경로를 감지) — 가이드가 말한 "집중 모드".

**프리플라이트**
룸에 들어가자마자 권한 창을 띄우지 않고, 먼저 카메라 미저장/STT 외부 처리 가능성/텍스트 모드 안내를
보여준 뒤 "마이크 테스트 시작"을 눌러야 실제로 권한을 요청합니다. 마이크가 안 되면 "텍스트 모드로
계속하기"로 바로 진입할 수 있습니다.

**답변 입력 · 꼬리질문 · 피드백**
- 질문은 로컬 `data/questions.json`에서 카테고리별로 불러와 매 세션 셔플, 실전 모드 첫 질문은
  `REAL_MODE_INTRO_QUESTION`(코드 고정) — 이전과 동일합니다.
- 답변 확정 시 `transcriptNormalizer`가 이미 승인된 개인 STT 보정 사전을 자동 적용한 뒤,
  `evaluateAnswer()`(규칙 기반 피드백)와 `followUpEngine`(꼬리질문 판단)을 거쳐 그 결과만
  `session_answers`에 저장됩니다 — 중간 인식 결과나 `MediaStream` 자체는 저장되지 않습니다.
- STAR/PREP 체크리스트는 보조 패널에서 답변을 입력하는 동안 실시간으로 갱신됩니다.

**화상/음성 녹화**
카메라 on/off, 화상 녹화, 음성 녹음은 `RoomControls` 하단 바에 있으며, 이전과 동일하게 **로컬 저장
전용**(서버 업로드 없음)입니다. 카메라가 꺼져 있으면 화상 녹화 버튼이 비활성화됩니다.

### 3-5. 세션 리포트 (`/interview/result/[sessionId]`)
- 답변 수 / 평균 답변 시간 / 필러 총합 / 평균 정중체 비율(추정치)을 통계 카드로 표시.
- 질문 텍스트는 Supabase 조인이 아니라 `lib/questionBank.ts`의 `getQuestionById()`로 로컬 데이터에서
  가져옵니다.
- Markdown 다운로드 버튼(`MarkdownExportButton`)으로 세션 전체를 `.md` 파일로 내보낼 수 있습니다.
- macOS Safari 스타일 창(`MacWindow`)에 담겨 있고, 결과 화면이라 매트릭스 배경도 함께 보입니다.

### 3-6. 마이페이지 (`/dashboard`)
- 지난 세션 목록(날짜/모드/답변 수, 클릭 시 리포트로 이동).
- 내 STT 보정 사전 목록 (`user_custom_terms`).
- 로그아웃 버튼.
- 페이지 진입 시, 보관기한이 지난 음성 로그(`audio_expires_at` 지난 행)를 클라이언트 접속 시점에
  자동 정리합니다(Supabase 무료 티어에 `pg_cron` 대신 채택한 방식). 다만 현재는 화상/음성이 서버에
  업로드되지 않으므로 이 컬럼은 사실상 비어 있습니다(§8 참고).

---

## 4. 로컬 데이터 파일 (git으로 관리, Supabase 아님)

### 4-1. `data/questions.json` — 질문 원본
```json
{
  "questions": [
    {
      "id": "team_project",
      "category": "technical",
      "expectedDurationSec": 90,
      "textJa": "チームで取り組んだプロジェクトについて教えてください。",
      "tags": ["team_project"]
    }
  ]
}
```
- `id`: 영문 slug, 다른 질문과 겹치면 안 됨. `follow_ups.txt`에서 이 id로 참조.
- `category`: `personality | technical | culture_fit | reverse`
- `expectedDurationSec`: 권장 답변 시간(초), 타이머/짧은 답변 판정 기준.
- `textJa`: 일본어 질문만 있습니다 (JLPT 난이도 태그, 한국어 번역 없음 — §3-2 참고).
- 현재 14개 질문(메인 10개 + 꼬리질문 대상 4개)이 예시로 들어 있습니다.

### 4-2. `public/data/follow_ups.txt` — 꼬리질문 규칙 (일반 텍스트)
```
# 형식: 원래질문id | 감지할단어1,감지할단어2,... | 다음질문id | 우선순위(선택)
strength | チーム,協力 | role | 1
team_project | チーム,プロジェクト,担当 | role_detail | 1
team_project | Python,SQL,機械学習,データ | tech_reason | 2
problem_solving | エラー,失敗,課題 | how_solved | 1
```
- `#`으로 시작하는 줄과 빈 줄은 무시.
- `public/` 아래에 있어 브라우저가 `fetch('/data/follow_ups.txt')`로 직접 읽습니다 — 다른 곳을
  안 건드리고 이 파일만 고쳐도 꼬리질문 규칙을 바로 추가/수정할 수 있습니다.
- 파싱/매칭 로직: `lib/questionBank.ts`(읽기·파싱) → `lib/followUp.ts`(매칭).

**다른 탭에서 예상질문·꼬리질문을 작성하실 때는 이 두 파일(`data/questions.json`,
`public/data/follow_ups.txt`)만 편집하시면 됩니다.** 편집 후 GitHub Desktop으로 커밋/푸시하면
Vercel이 자동 재배포합니다. Supabase SQL을 다시 실행할 필요는 없습니다.

---

## 5. Supabase에 실제로 저장되는 것 (개인별 기록만)

| 테이블 | 용도 |
|---|---|
| `sessions` | 면접 세션 1건 (사용자, 모드, 시작 시각) |
| `session_answers` | 질문/꼬리질문별 답변 텍스트, 시간, 피드백 결과(JSON), STT 원문/수정본 |
| `user_settings` | 레벨 체크 결과, 경어 모드, 기본 설정 |
| `diagnostic_results` | 레벨 체크 진단 이력 |
| `user_custom_terms` | 사용자별 STT 오인식 보정 사전 |

`questions` / `follow_up_rules` 테이블은 더 이상 사용하지 않습니다(§4 참고). 기존에 구버전으로
만들어 둔 프로젝트는 `supabase/migrate_local_questions.sql`을 1회 실행해 정리합니다.

---

## 6. 디자인 시스템 요약

| 컴포넌트 | 용도 |
|---|---|
| `MacWindow` | macOS Safari 스타일 2단 창(트래픽 라이트 타이틀바 + 주소창 pill 툴바). 레벨체크/모드선택/결과/마이페이지 공통 래퍼. |
| `SiteChrome` | 사이트 공통 상단바/푸터. `/interview/run/*` 에서는 자동으로 숨겨 면접실을 전체 화면 "집중 모드"로 만듦 |
| `HeroCard` | 홈 화면의 기울어진 호버 타이틀 카드 |
| `PillRadio` | 슬라이딩 인디케이터가 있는 세그먼트 버튼 (JLPT/경어/장음 선택 등) |
| `MicToggle` | 오디오 스펙트럼 느낌의 마이크 on/off 토글 (레벨 체크의 장음/경어 발음 테스트용) |
| `LoadingDots` | 통통 튀는 점 3개 로더 |
| `MatrixBackground` | 카타카나 낙하 배경. `/interview/run/*` 에서는 자동 숨김 |
| `LogoutButton` | 로그아웃 |
| `SupportBanner` | 브라우저 STT/TTS 지원 배지 |
| `MarkdownExportButton` | 세션 리포트 Markdown 다운로드 |

면접 진행 화면 전용 컴포넌트(`PreflightDialog`/`RoomHeader`/`InterviewStage`/`SelfPreview`/
`TranscriptPanel`/`CoachingPanel`/`RoomControls`/`InterviewRoom`)는 `features/interview/components/`에
있으며 §3-4에 정리했습니다. 이전 버전의 `CameraPreview`(드래그·리사이즈 플로팅 창)와
`ZoomControlBar`, `WaveformVisualizer`는 이 리팩터링으로 `SelfPreview`/`RoomControls`/
`useAudioLevel`에 흡수되어 삭제되었습니다.

전역 스타일은 전부 `app/globals.css` 한 파일에 있습니다 (Tailwind 등 별도 프레임워크 없음).

---

## 7. 지금 다른 탭에서 작업하실 때 참고할 경로 요약

```
data/questions.json                 ← 질문 추가/수정 (JSON)
public/data/follow_ups.txt          ← 꼬리질문 규칙 추가/수정 (일반 텍스트)
```
두 파일 다 이 저장소(git) 안에 있고, Supabase나 코드 변경 없이 이 파일들만 고쳐도 앱에 바로
반영됩니다(로컬 개발 서버는 저장 즉시, 배포본은 커밋/푸시 후 자동 재배포).

---

## 8. 알려진 제한사항 (요약, 자세한 내용은 SETUP.md)

> ⚠️ 이 절은 2026-09-02 기준으로 갱신했습니다. §2(인증)·§3-2(레벨 체크)·아래 표의 일부
> 항목은 그 사이 진행된 개편(레벨 체크 기능 제거, 공유 비밀번호 → 고유 번호 로그인 전환 등)을
> 아직 반영하지 못한 채 남아있습니다 — 현재 동작 기준은 log/ 폴더의 날짜별 작업 로그와
> SETUP.md를 우선 참고하세요.

- 화상/음성 녹화는 의도적으로 서버에 올리지 않음 (로컬 다운로드만).
- 적응형 난이도 자동 제안, 레이더 차트/히트맵 등 고급 시각화는 미구현.
- 카메라 미리보기는 "셀프 뷰"일 뿐 실제 화상통화(면접관 쪽 영상)는 아님.
- 레벨 체크 기능은 평가 기준이 없어 완전히 제거되었습니다(2026-09-02, 아래 §10 참고).
- 공유 비밀번호는 제거되고, 고유 번호 기반 로그인(다른 기기에서도 같은 번호로 기록을
  이어볼 수 있음)으로 대체되었습니다 — 단, 번호 자체가 사실상 비밀번호 역할을 겸합니다.

---

## 9. "Zoom형 프런트엔드 가이드"(Manus) 대비 현재 구현 상태 — 전면 리팩터링 완료 (2026-09-01)

이전 버전(§9 초안)에서 "없음"으로 표시했던 항목들을 이번에 전면 리팩터링하며 대부분 구현했습니다.

| 가이드의 개념 | 현재 상태 | 비고 |
|---|---|---|
| 중앙 면접관 스테이지 + 질문 | ✅ 있음 | `InterviewStage`(`.room-stage`) |
| 셀프 카메라/파형 미리보기 | ✅ 있음 (도킹형으로 재구현) | `SelfPreview`. 이전의 드래그·리사이즈 플로팅 창은 가이드의 도킹형 레이아웃으로 대체 |
| 하단 고정 컨트롤 바 | ✅ 있음 | `RoomControls` — 마이크/카메라/자막/패널/다시듣기/화상녹화/음성녹음 + 답변 완료·다음 질문 + 종료를 한 바로 통합 |
| 우측 보조 패널(전사/STAR/메모) | ✅ 있음 | `TranscriptPanel`(전사 편집) / `CoachingPanel`(STAR 체크·규칙 신호) / 메모(비영속) 3탭 |
| 명시적 상태 머신(`InterviewPhase`) | ✅ 있음 (일부 단순화) | `useInterviewMachine`. `followUpReady`/`fallbackText`는 별도 화면 없이 기존 phase 흐름에 흡수(§ 아래 설명) |
| 프리플라이트(장치 확인 다이얼로그) | ✅ 있음 | `PreflightDialog` — 개인정보 고지 + "마이크 테스트 시작" 이후에만 권한 요청 |
| `features/interview` 폴더 분리 | ✅ 있음 | 가이드의 `src/features/interview` 구조를 `features/interview`로 그대로 도입 (프로젝트에 `src/` 프리픽스가 없어 그 차이만 남음) |
| 텍스트 모드 폴백 | ✅ 있음 | 마이크 미지원/거부 시 `TranscriptPanel`에 안내 배지 + 직접 입력 |
| TTS 재생 중 마이크 자동 시작 금지 | ✅ 있음 | `interviewerSpeaking` phase에서 마이크 버튼 비활성화, `useSpeechRecognition`은 phase==="listening"일 때만 켜짐 |
| STT 인식 종료 시 조건부 재시작 | ✅ 있음 | `desiredRef`/의도적 종료 여부로 무한 재시작·조기 재시작 방지 |
| 모바일/태블릿 반응형 레이아웃(가이드 §2 표) | ✅ 있음 (CSS만 검증, 실기기 확인 필요) | 1280px/768px/480px 미디어쿼리로 구현. 이 세션의 브라우저 자동화 환경은 뷰포트 리사이즈가 실제로 반영되지 않아 **육안 확인은 데스크톱 폭에서만** 했습니다 |
| 답변 확정 시점에만 서버 저장 | ✅ 있음 | `MediaStream`/`SpeechRecognition` 인스턴스는 `useRef`로만 보관, `session_answers`에는 확정된 텍스트/피드백만 insert |
| 개인정보 고지 문구(카메라 미저장 등) | ✅ 있음 | `PreflightDialog` 안에 직접 표시 (이전에는 SETUP.md에만 있었음) |
| 사용자 보정 사전 자동 정규화(`transcriptNormalizer`) | ✅ 있음 (신규) | 가이드에는 명시되지 않았지만, 이미 승인된 STT 보정 사전을 확정 시점에 자동 적용하도록 추가 구현 |
| Zustand | ❌ 미채택 (의도적) | 새 의존성 추가 없이 React 로컬 state로 UI 상태(패널 열림/자막/탭)를 분리했습니다. 목적(서버 상태와 UI 상태 분리)은 동일하게 달성 |

**의도적으로 단순화한 부분**
- `followUpReady`는 별도의 화면 대기 상태 없이, 저장 직후 곧바로 다음(꼬리)질문의 `questionReady`로
  전환됩니다. 사용자 입장에서는 "꼬리 질문" 배지가 바뀐 질문 카드로 바로 나타나는 것과 동일합니다.
- `fallbackText`는 별도 phase 분기 대신 `sttSupported` 플래그로 처리했습니다. TTS는 정상적으로
  계속 재생되고, 마이크 관련 컨트롤만 비활성화되어 `TranscriptPanel`에 항상 직접 입력할 수 있습니다.

**남은 검증 항목**
- 태블릿/모바일 실기기에서의 최종 레이아웃 확인(2026-09-02에 390px 폭 iframe으로 1차 확인
  완료 — 아래 §10 참고, 실기기 확인은 아직 필요).
- 접근성(스크린리더, 키보드 전용 탐색) 수동 점검 — `aria-pressed`/`aria-live`/`aria-label`은
  코드에 반영했으나 실제 스크린리더로 검증하지는 못했습니다.
- 실제 Supabase 프로젝트를 연결한 전체 흐름(고유 번호 로그인→면접→결과) 종단 테스트.

---

## 10. 2026-09-02 작업 요약

이날 진행한 작업의 자세한 내용은 `log/2026-09-02_작업로그.txt`에 있습니다. 핵심만 요약합니다.

- **모바일 반응형 개선**: 보조 패널(대화/STAR/메모)이 좁은 화면(768px 미만)에서는 기본적으로
  닫힌 채 시작하도록 했고, 열렸을 때는 바깥을 탭하면 닫히는 배경과 "✕" 닫기 버튼을 추가했습니다.
  하단 컨트롤 바도 좁은 화면에서 줄바꿈되도록 손봤습니다. 실제 브라우저 리사이즈가 반영되지
  않는 이 자동화 환경 대신 390px 폭 iframe으로 레이아웃과 상호작용(배경 탭 시 닫힘 등)을
  검증했습니다 — 실기기에서의 최종 확인은 아직 필요합니다.
- **질문 은행 대폭 확충**: 실제 일본 IT 면접 빈출 주제(Git 충돌 해결, CI/CD, 장애 대응,
  기술 부채, 전직 사유, 부업, 유급휴가, 재류자격 등) 28개 대분류 질문 + 꼬리질문 5개를
  추가해 총 164개(대분류 103 + 꼬리질문 56 + 마무리 5)가 되었습니다. `QuestionBankExplained.md`도
  최신 숫자로 갱신했습니다. (이후 batch6·batch7까지 이어서 총 210개까지 확충 — 자세한 내용은
  로그 파일 참고.)
- **고유 번호 이메일 형식 오류 수정**: 가짜 이메일 도메인이 `.local`(예약된 TLD)이라
  Supabase가 거부하던 문제를 `voiceinterviewjp-users.com`으로 바꿔 해결했습니다.
- **대화창(전사) 완전 삭제 불가 버그 수정**: 빈 문자열로 지워도 직전 인식 중간값으로
  다시 채워지던 문제를 고쳐, 이제 키보드로 완전히 비울 수 있습니다.
- **STT 자동 문장부호**: 말이 끊길 때마다(인식 엔진의 "final" 확정 시점) 일본어 마침표(。)를
  자동으로 붙여, 전사가 더 읽기 쉽고 채점 로직의 문장 분리 정확도도 함께 좋아졌습니다.
- **결과 화면 질문별 답변 시간 비교**: "답변 75초 / 목표 60초 · 길어요"처럼 목표 시간
  대비 실제 답변 시간을 색깔 배지로 보여줍니다.
- **헤더에 질문 은행 전체 개수 표시**: "질문 3 / 18 (전체 210개)"처럼 현재 세션 진행률
  옆에 전체 질문 은행 규모를 함께 보여줍니다(좁은 화면에서는 자동으로 숨겨 오버플로를 방지).
- **면접관 발화 속도 조절(+/-/기본값)**: 브라우저 기본 음성과 VOICEVOX 외부 음성 모두에
  적용되는 배속 조절(0.5~1.4배, 기본 0.95배)을 면접 화면에 추가했습니다.
- **요미가나식 실시간 낭독 위치 표시**: TTS가 지금 읽고 있는 글자를 초록색으로 강조하고
  그 위에 작은 점을 얹어 보여줍니다(브라우저 기본 음성에서만 동작, VOICEVOX는 미지원).
- **음성 파형(웨이브폼) 시각화 추가**: 사용자가 보내주신 참고 이미지를 바탕으로, 실제
  마이크 레벨에 반응하는 24개 막대 파형을 면접 화면에 추가했습니다(별도 분석 라이브러리
  없이 0원 운영 원칙 유지, 색상은 참고 이미지의 파란색 대신 기존 초록 테마로 각색).
- **"듣고 있습니다" 초록 점 확대**: 다른 텍스트와 겹치지 않는 선에서 10px → 22px로 키웠습니다.
- **오픈소스 STT(DeepSpeech/Leon/Wav2letter++/Annyang/SpeechRecognition) 검토 후 미채택**:
  대부분 서버 실행이 필요해 0원 운영 원칙과 맞지 않고, Annyang은 이미 쓰고 있는 브라우저
  내장 엔진을 감싸는 래퍼일 뿐이라 실질적 이득이 없다고 판단했습니다. 자세한 이유는
  로그 파일 참고.
- **(자체 발견 및 수정) 좁은 화면 헤더 오버플로**: 위 "전체 개수 표시" 추가로 320~375px
  폭에서 헤더 한 줄이 넘칠 수 있던 문제를 480px 이하에서 전체 개수를 숨기도록 고쳤습니다.

**이번 회차에서 사용자 검토를 권하는 부분**
- 파형 색상(현재 초록)을 참고 이미지처럼 파란색으로 바꾸고 싶으면 `app/globals.css`의
  `.voice-waveform.active .voice-waveform-bar` 한 줄만 바꾸면 됩니다.
- 발화 속도의 기본값(0.95배)·조절 범위(0.5~1.4배)가 적당한지 실제 사용 후 알려주시면
  조정하겠습니다.
